_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor

# Phase 4 (rebrand-p4) a11y/contrast verification tooling (scripts/, package.json) --
# node_modules is local/CI-installed, never committed. package-lock.json IS
# committed (npm ci reproducibility in the Phase 4b GitHub Actions workflow).
node_modules

# <!-- eidolon:gitignore start -->
# Managed by eidolons. Recreated by `eidolons sync`. Do not edit between markers.
/.eidolons/*
!/.eidolons/cortex/
!/.eidolons/cortex/**
!/.eidolons/harness/
!/.eidolons/harness/manifest.json
# <!-- eidolon:gitignore end -->
.atlas/

# Per-machine Claude Code permissions/MCP toggles -- not project state.
.claude/settings.local.json

# Visual-regression baselines (scripts/visual-baseline.mjs) -- ~38 MB of PNGs,
# regenerated on demand, never source. Capture from a clean tree, apply the
# refactor, then diff:
#   git stash && npm run test:visual:update && git stash pop && npm run test:visual
scripts/baselines/
